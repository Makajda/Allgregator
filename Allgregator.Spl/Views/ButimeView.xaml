<UserControl x:Class="Allgregator.Spl.Views.ButimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:common="clr-namespace:Allgregator.Aux.Common;assembly=Allgregator.Aux"
             xmlns:converters="clr-namespace:Allgregator.Aux.Converters;assembly=Allgregator.Aux"
             x:Name="rootElement"
             >

    <UserControl.Resources>
        <converters:ValueMaxToHeightConverter x:Key="ValueMaxToHeightConverter"/>
        
        <Style x:Key="ctrlButton" TargetType="Button">
            <Setter Property="Grid.Row" Value="1"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="1"/>
            <Setter Property="common:Shaatt.Shape" Value="Rect"/>
        </Style>
        
        <DataTemplate x:Key="ItemTemplate">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Button
                    Grid.ColumnSpan="3" common:Shaatt.Shape="TopCap" VerticalAlignment="Bottom"
                    Background="GreenYellow" Margin="1,0,1,0" Content="{Binding Name}" BorderThickness="0">
                    <Button.Height>
                        <MultiBinding Converter="{StaticResource ValueMaxToHeightConverter}">
                            <Binding Path="DataContext.MaxValue" ElementName="rootElement"/>
                            <Binding Path="Value"/>
                            <Binding Path="RowDefinitions[0].ActualHeight" RelativeSource="{RelativeSource AncestorType=Grid}"/>
                        </MultiBinding>
                    </Button.Height>
                </Button>
                
                <Button Grid.Column="0" Content="-15" Background="GreenYellow" Style="{StaticResource ctrlButton}"/>
                <Button Grid.Column="1" Content="+15" Background="GreenYellow" Style="{StaticResource ctrlButton}"/>
                <Button Grid.Column="2" Content="+30" Background="GreenYellow" Style="{StaticResource ctrlButton}"/>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <ItemsControl ItemsSource="{Binding Butasks}" ItemTemplate="{StaticResource ItemTemplate}" VerticalAlignment="Bottom">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <UniformGrid Columns="{Binding Butasks.Countfy}"/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
    </ItemsControl>
</UserControl>
