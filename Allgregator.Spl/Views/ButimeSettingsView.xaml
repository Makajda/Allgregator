<UserControl x:Class="Allgregator.Spl.Views.ButimeSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:common="clr-namespace:Allgregator.Aux.Common;assembly=Allgregator.Aux"
             xmlns:converters="clr-namespace:Allgregator.Aux.Converters;assembly=Allgregator.Aux"
             x:Name="rootElement"
             >

    <UserControl.Resources>
        <Style x:Key="clrStyle" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="common:Shaatt.Shape" Value="Rect"/>
        </Style>
        
        <DataTemplate x:Key="ButaskItemTemplate">
            <Grid Height="62" Margin="0,1,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Text="{Binding Name}" Margin="30,0,10,0" VerticalAlignment="Center" Padding="8" FontSize="16" BorderThickness="0"/>
                <StackPanel Grid.Column="1" Margin="1">
                    <StackPanel Orientation="Horizontal">
                        <Button common:Shaatt.Shape="Rect" Background="Red" Style="{StaticResource clrStyle}"/>
                        <Button common:Shaatt.Shape="Rect" Background="Orange" Style="{StaticResource clrStyle}"/>
                        <Button common:Shaatt.Shape="Rect" Background="Yellow" Style="{StaticResource clrStyle}"/>
                        <Button common:Shaatt.Shape="Rect" Background="Green" Style="{StaticResource clrStyle}"/>
                        <Button common:Shaatt.Shape="Rect" Background="Blue" Style="{StaticResource clrStyle}"/>
                        <Button common:Shaatt.Shape="Rect" Background="DarkBlue" Style="{StaticResource clrStyle}"/>
                        <Button common:Shaatt.Shape="Rect" Background="Violet" Style="{StaticResource clrStyle}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Button common:Shaatt.Shape="Rect" Background="Purple" Style="{StaticResource clrStyle}"/>
                        <Button common:Shaatt.Shape="Rect" Background="Orchid" Style="{StaticResource clrStyle}"/>
                        <Button common:Shaatt.Shape="Rect" Background="YellowGreen" Style="{StaticResource clrStyle}"/>
                        <Button common:Shaatt.Shape="Rect" Background="IndianRed" Style="{StaticResource clrStyle}"/>
                        <Button common:Shaatt.Shape="Rect" Background="Brown" Style="{StaticResource clrStyle}"/>
                        <Button common:Shaatt.Shape="Rect" Background="Cyan" Style="{StaticResource clrStyle}"/>
                        <Button common:Shaatt.Shape="Rect" Background="Wheat" Style="{StaticResource clrStyle}"/>
                    </StackPanel>
                </StackPanel>

                <Button Grid.Column="2" common:Shaatt.Shape="Rect" Content="up" Margin="1" Padding="10"/>
                <Button Grid.Column="3" common:Shaatt.Shape="Rect" Content="dwn" Margin="1" Padding="10"/>
                <Button Grid.Column="4" common:Shaatt.Shape="Circle" Content="del" Margin="1" Padding="10"/>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="0,10,0,0">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Data.Mined.Butasks}" ItemTemplate="{StaticResource ButaskItemTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>

        <!--//todo<Popup StaysOpen="False" Placement="Mouse" x:Name="popupOpen">
            <ListBox x:Name="listOpen" ItemTemplate="{StaticResource OpenItemTemplate}"/>
        </Popup>-->
    </Grid>
</UserControl>
