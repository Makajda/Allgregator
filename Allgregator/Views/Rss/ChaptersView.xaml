<UserControl x:Class="Allgregator.Views.Rss.ChaptersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"  
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:views="clr-namespace:Allgregator.Views.Rss"
             >

    <DockPanel>
        <TextBlock DockPanel.Dock="Left"
                   Text="Rss"
                   FontSize="48" 
                   FontStretch="ExtraExpanded"
                   FontFamily="Comic"
                   VerticalAlignment="Center" 
                   Margin="50,0,30,0"
                   Foreground="Green"
                   />

        <ScrollViewer VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Auto" HorizontalAlignment="Right">
            <ItemsControl ItemsSource="{Binding Chapters}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <views:ChapterView />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
